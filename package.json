{
  "name": "@einsteins/zentao-v1-mcp",
  "version": "1.0.0",
  "description": "MCP server for ZenTao RESTful API v1",
  "license": "Apache-2.0",
  "homepage": "https://github.com/xuansheep/zentao-v1-mcp#readme",
  "bugs": {
    "url": "https://github.com/xuansheep/zentao-v1-mcp/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xuansheep/zentao-v1-mcp.git"
  },
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "zentao-v1-mcp": "dist/cli.js"
  },
  "files": [
    "dist",
    "doc",
    "README.md",
    "README.en.md",
    "LICENSE",
    "NOTICE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "smoke": "vitest run tests/smoke/stdio-smoke.test.ts",
    "verify": "npm run build && npm test && npm run smoke && npm audit --audit-level=moderate && npm pack --dry-run",
    "prepack": "npm run build",
    "prepublishOnly": "npm run build && npm test && npm run smoke && npm audit --audit-level=moderate"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.7"
  },
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  },
  "optionalDependencies": {
    "playwright": "^1.61.1"
  }
}
